---
sidebar_position: 1
sidebar_label: rclpy CLI Tools
---

# rclpy CLI Tools

Master the essential ROS 2 command-line tools for `rclpy` nodes. This chapter covers `ros2 run`, `ros2 topic`, `ros2 node`, `ros2 service`, and `ros2 param` for inspecting and interacting with your ROS 2 system.
